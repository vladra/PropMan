%b Tenant :
= @issue.tenant.first_name
%br
%b Title :
= @issue.title
%br
%b Message :
= @issue.message
%br
%b Complete Date :
= @issue.complete_date
%br
%b Status :
= @issue.status
%br
%b Rating :
= @issue.rating
%br
%b Comments:
= @issue.comments.count
%br
%b Message
- @issue.comments.each do |comment|
	%b Message :
	= comment.message
	%br
	%b By:
	%br
/ = form_for @comment do |f|
/ 	= f.label :message
/ 	%br
/ 	%br
/ 	= f.text_area :message
/ 	%br
/ 	%br
/ 	= f.submit

/ = form_for([@issue.comments.build]) do |f|
/ 	= if @comment.errors.any?
/ 		%div id="explanation"
/ 		%h1 Pluralize(@comment.errors.count, "error")
/ 		%b Prohibbitted this comment from being saved
/ 		%ul @comment.errors.full_messages.each do |msg|
/ 		%li msg
= form_for([@issue, @issue.comments.build]) do |f|
	%b Add a comment:
	%br
	= f.label :comment
	%br
	= f.text_area :message
	%br
	= f.submit

















